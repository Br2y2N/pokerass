{
  "name": "poker-clock",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de torneios de poker — HTML/CSS/JS puro, sem build, sem framework.",
  "private": true,
  "scripts": {
    "test": "node test/smoke.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
